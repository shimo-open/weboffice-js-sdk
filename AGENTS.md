# 项目开发规范

## 基本原则

- 修改前先检查相关代码、配置、测试和当前工作区状态。
- 保留用户已有的修改，不覆盖、回退或清理无关改动。
- 优先进行范围最小、容易审查和容易回滚的修改。
- 遵循项目现有的目录结构、命名方式、代码风格和依赖版本。
- 不因为单个需求改变整个项目的通用行为；专项规则应放在专项文档或对应模块中。

## 接口和类型变更

- 修改公共接口时，同时检查实现、类型声明、兼容行为、测试和文档。
- 已发布的接口、属性和行为默认保持兼容；删除或重命名需要明确的迁移方案。
- 优先复用已有类型和工具，避免重复定义相同概念。
- 对外暴露的数据应保持稳定，内部实现细节不应泄露到公共接口。

## 测试和验证

根据修改范围运行必要检查：

```bash
npm test
npm run lint
npx tsc -p tsconfig.json --noEmit --pretty false
npm run build
```

- 修改测试相关代码时，必须运行对应的定向测试。
- 修改公共接口时，必须验证类型和兼容行为。
- 涉及页面或运行时行为时，除自动化测试外，还应进行适当的手工或真实环境验证。
- 不要只根据“请求成功”判断功能完成，应根据实际需求验证可观察结果。

## 文档和提交

- 公共行为变化要同步更新对应文档。
- 专项设计、实验记录和环境说明放在 `doc/` 或对应模块文档中，不写入本文件。
- 不擅自执行 `git add`、提交、推送或删除用户文件，除非用户明确要求。
- 完成后说明修改范围、验证结果和仍存在的限制。
